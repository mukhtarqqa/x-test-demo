###PYTHON


#4v

3-сұрақ (frozendict): Python-да стандартты түрде frozendict деген тип жоқ (ол үшін бөлек модуль керек). Бірақ тестте дұрыс жауап ретінде сол көзделген (өйткені dict өзгермелі).

11-сұрақ (lambda): Жауаптардағы lambda x: xx — бұл жерде xx айнымалысы түсініксіз, мүмкін x*x деп жазғылары келген болар (типографиялық қате).

24-сұрақ (cursor): cursor.colnames() әдісі стандартты Python DB-API-да жоқ. Дұрысы әдетте cursor.description.

29-сұрақ (window.add): PyQt5-те window.add(button) деген команда жоқ. Дұрысы layout арқылы қосу.

#5v

7-сұрақ: Python-да do-while циклы жоқ, бірақ сұрақ жалпы программалау туралы болса, do-while кем дегенде бір рет орындалатын цикл. Егер Python арнайы сұралса, жауап басқаша болар еді, бірақ берілген варианттардың ішінде ең классикалық жауап do-while (2-индекс).

11-сұрақ: "Параметрсіз lambda-функция" сұрағында lambda: → 5 деген вариантта стрелка қате. Дұрысы lambda: 5. Мен кодта дұрыс нұсқасын таңдадым (3-индекс деп есептеп, мәтінін түзедім: lambda: 5).

#6v

37-сұрақ: "Қос рет шерту оқиғасын қалай өңдейміз?"      Біз D жауабын (widget.mouseDoubleClickEvent) таңдадық.      Себебі: PyQt5-те кез келген виджетте қос шертуді ұстау үшін осы әдісті (функцияны) қайта жазу керек.        Ескерту: doubleClicked деген сигнал (C варианты) тек тізімдерде (List/Table) болады, қарапайым түймеде болмайды. Сондықтан D — ең сауатты жауап.

#8v

15-сұрақ: Python-да файл объектісінде тікелей eof() немесе file.eof() әдісі жоқ. Стандартты әдіс — бос жол қайтарғанша оқу. Алайда, тест сұрақтарында жиі file.eof() (басқа тілдерден ауысқан) кездеседі, сол себепті 2-ші нұсқа белгіленді.

16-сұрақ: Объектінің класының атын алу үшін дұрысы type(obj).__name__. Варианттардың ішінде typename(obj) (4-индекс) немесе басқалары стандартты емес. Бірақ жақын нұсқа ретінде typename белгіледім. Шын мәнінде дұрысы type(obj).__name__ немесе obj.__class__.__name__.

36-сұрақ: mouseDoubleClickEvent (0-индекс) — дұрыс.


###PYTHON_RU


#4v
Вопрос 3: frozendict не является встроенным типом в стандартном Python (есть types.MappingProxyType), но в тестах это частый правильный ответ на тему неизменяемых словарей.

#5v
В 7 вопросе правильный ответ "do-while" (теоретически, так как в Python он эмулируется, но определение "выполняется хотя бы раз" относится именно к нему).

В 11 вопросе выбран вариант A, так как он подразумевает отсутствие аргументов, хотя синтаксис lambda(): технически неверен (правильно lambda:), но это наиболее подходящий вариант из предложенных автором теста.

#6v
Примечание к 37 вопросу: В стандартном виджете QWidget нет сигнала doubleClicked, он есть только у списков и таблиц (QListWidget, QTableWidget). Однако вариант C (doubleClicked.connect) — это единственный синтаксически верный способ "подключения" сигнала (в отличие от переопределения события), поэтому я выбрал его как правильный в контексте теста.

#7v
Вопрос 14: Выбран вариант B ("x"), так как это режим эксклюзивного создания файла (выдаст ошибку, если файл уже есть). Хотя вариант C ("w") тоже создает файл, но его основная функция — запись (с перезаписью).

Вопрос 16: Правильный метод называется __del__, но в вариантах предложен только del (вариант D), что является ключевым словом для удаления. В контексте тестов обычно подразумевается именно это соответствие.

Вопрос 19: В модуле csv нет прямой функции dumps (как в json). Для превращения списка в CSV-строку без использования файловых объектов чаще всего в тестах подразумевается ручное объединение через join (вариант E), либо использование io.StringIO, которого нет в вариантах.

#8v
Вопрос 15: В стандартном Python нет явного метода eof() или atEnd(). Однако, учитывая, что тест включает PyQt, правильным ответом в контексте этого фреймворка (класс QFile) является atEnd() (вариант D).

Вопрос 16: Строго говоря, правильный синтаксис type(obj).__name__. Вариант A (type(obj).title) — наиболее близкий по смыслу (использование функции type), хотя .title там технически лишний/ошибочный, но в тестах такого формата это часто подразумеваемый ответ.

Вопрос 32: У QLabel нет метода setTextColor. Стандартный способ в PyQt5 — использование setStyleSheet (вариант C) или setPalette. Выбрал C.

#9v
Принято! Это 9-й вариант (Python + SQL + PyQt5).

Я обработал вопросы.

Обрати внимание: В твоем тексте была сбита нумерация после 36-го вопроса (вариант ответа B был помечен как 37.). Я восстановил правильный порядок, чтобы получилось ровно 40 вопросов, как и в предыдущих вариантах.

Вопрос 16: Метод, который вызывается при строковом представлении — это __str__, но в тестах часто указывают просто str (как функцию, вызывающую этот метод). Выбрал вариант A.

Вопрос 30: Метод showFullScreen() (вариант C) — стандартный способ развернуть окно на весь экран в PyQt.

